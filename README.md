# Github_to_Elasticsearch
Basically a faster but incomplete version of octokit written in bash to poll specific data and load it into elasticsearch in a specific way so that records are never duplicated. 
